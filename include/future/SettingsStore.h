#pragma once

// Placeholder for future NVS-backed settings.
// Planned settings include shunt resistance, calibration, zero offset,
// display behavior, Wi-Fi mode, BLE name, and alarm thresholds.
class SettingsStore
{
public:
    bool begin() { return true; }
};
